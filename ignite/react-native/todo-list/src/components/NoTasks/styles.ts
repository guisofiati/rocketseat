import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    marginTop: 40,
    alignItems: "center",
    textAlign: "left"
  },
  textTitle: {
    color: "#808080",
    fontWeight: "bold"
  },
  textParagraph: {
    color: "#808080",
  }
})